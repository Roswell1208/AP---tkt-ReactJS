import React,  {useEffect} from 'react';

const Logout = () => {
    useEffect(() => {
        document.title = 'Logout';
    }, []);

    return (
        <div>
            
        </div>
    );
};

export default Logout;